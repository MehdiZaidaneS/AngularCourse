import { ButtonColor } from './button-color';

describe('ButtonColor', () => {
  it('should create an instance', () => {
    const directive = new ButtonColor();
    expect(directive).toBeTruthy();
  });
});
